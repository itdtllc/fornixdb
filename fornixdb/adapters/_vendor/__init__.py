"""Vendored third-party code. Each subpackage keeps its upstream LICENSE and a
provenance note in its files. Nothing here is imported unless an optional
adapter (e.g. the [mac] watch loop) needs it."""
